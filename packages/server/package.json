{
  "name": "@glide/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "pnpm --filter @glide/shared build && tsx watch src/index.ts",
    "build": "pnpm --filter @glide/shared build && tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@glide/shared": "workspace:*",
    "@colyseus/schema": "^2.0.0",
    "@colyseus/ws-transport": "^0.15.0",
    "colyseus": "^0.15.0",
    "express": "^4.19.0",
    "planck": "^1.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "tsx": "^4.7.0"
  }
}
